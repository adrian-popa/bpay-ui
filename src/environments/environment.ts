// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyB-Ik8lA940Ywb6LmDaGe-vkW9OSvMlOmw',
    authDomain: 'bpay-225c1.firebaseapp.com',
    databaseURL: 'https://bpay-225c1.firebaseio.com',
    projectId: 'bpay-225c1',
    storageBucket: 'bpay-225c1.appspot.com',
    messagingSenderId: '912111041138',
    appId: '1:912111041138:web:8c9154c880a5c978'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
